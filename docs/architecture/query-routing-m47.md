# Decision record — M47 Query routing

**Date:** 2026-08-05  
**Status:** adopted

Rule-based `QueryRouter`: skip_retrieval | dense_sparse | graph (if ready).
Default fallback dense_sparse. No hosted LLM classifier. Eval: `eval_routing`.
